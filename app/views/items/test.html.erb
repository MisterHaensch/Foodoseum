<h1 class="current-category"> <%= params[:category] %> </h1>

<% if @items_drinks.count == 0 %>
    <h3>There are no items in this category</h3>
<% else %>
    <div class="row">
        <h2> Drinks </h2>
        <% @items_drinks.each do |item| %>
        <div class="col-sm-4" style="border: 1px solid black">
            <div><%= link_to image_tag(item.image, height: 200, width: 200), item_path(item) %></div>    
            <h4> <%= link_to item.title, item_path(item) %> </h4>
        </div>   
        <% end %>
    </div>

    <div class="row">
        <h2> Cleaning Agents </h2>
        <% @items_cleaning.each do |item| %>
        <div class="col-sm-4" style="border: 1px solid black">
            <div><%= link_to image_tag(item.image, height: 200, width: 200), item_path(item) %></div>    
            <h4> <%= link_to item.title, item_path(item) %> </h4>
        </div>   
        <% end %>
    </div>
    
    <div class="row">
        <h2> Frozen Food </h2>
        <% @items_frozen.each do |item| %>
        <div class="col-sm-4" style="border: 1px solid black">
            <div><%= link_to image_tag(item.image, height: 200, width: 200), item_path(item) %></div>    
            <h4> <%= link_to item.title, item_path(item) %> </h4>
        </div>   
    <% end %>
    
    <div class="row">
        <h2> Cookies </h2>
        <% @items_cookies.each do |item| %>
        <div class="col-sm-4" style="border: 1px solid black">
            <div><%= link_to image_tag(item.image, height: 200, width: 200), item_path(item) %></div>    
            <h4> <%= link_to item.title, item_path(item) %> </h4>
        </div>   
        <% end %>
    </div>

    <div class="row">
        <h2> Beauty Products </h2>
        <% @items_beauty.each do |item| %>
        <div class="col-sm-4" style="border: 1px solid black">
            <div><%= link_to image_tag(item.image, height: 200, width: 200), item_path(item) %></div>    
            <h4> <%= link_to item.title, item_path(item) %> </h4>
        </div>   
        <% end %>
    </div>
    
    <div class="row">
        <h2> Ice Cream </h2>
        <% @items_ice.each do |item| %>
        <div class="col-sm-4" style="border: 1px solid black">
            <div><%= link_to image_tag(item.image, height: 200, width: 200), item_path(item) %></div>    
            <h4> <%= link_to item.title, item_path(item) %> </h4>
        </div>   
        <% end %>
    </div>
    
<% end %>



